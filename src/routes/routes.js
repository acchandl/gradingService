const student = require('./student');
const worksheet = require('./worksheet');
const worksheetQuestions = require('./worksheetQuestions');

module.exports = {
    student: student,
    worksheet: worksheet,
    worksheetQuestions: worksheetQuestions,
};
